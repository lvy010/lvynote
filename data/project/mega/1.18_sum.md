实现

- 引入独立 targets 表管理目标级元数据
- 增强状态追踪能力，支持目标级状态持久化
- 优化 API 设计，支持按目标查询和分组展示
- 保持向后兼容性

## 数据模型设计

新增 targets 表

- 设计包含 9 个字段的表结构（id, task_id, target_path, state, start_at, end_at, error_summary, created_at 等）
- 建立外键关系：targets.task_id → tasks.id，builds.target_id → targets.id
- 设计索引优化查询性能（task_id, state, created_at）

builds 表重构

- 将 target 字段（VARCHAR）替换为 target_id (UUID)
- 四步迁移，确保数据平滑过渡

SeaORM Entity 定义

- 规划 Model、Entity、Column、Relation 的实现结构
- 设计辅助方法（create_target, update_state 等）

## API 设计

新增端点

- GET /tasks/{task_id}/targets：获取 Task 的所有 Targets
- GET /targets/{target_id}/logs：获取 Target 日志（支持 full/segment 模式）

现有端点增强

- 调整 /tasks/{cl} 响应结构，新增 targets 字段按目标分组
- 保留 build_list 字段确保向后兼容
